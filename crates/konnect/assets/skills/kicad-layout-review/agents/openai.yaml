interface:
  display_name: "KiCad Layout Review"
  short_description: "Review PCB layout, buck loops, and ground paths"
  default_prompt: "Use $kicad-layout-review to audit this KiCad PCB layout and prioritize evidence-backed findings."

dependencies:
  tools:
    - type: "mcp"
      value: "kicad"
      description: "Konnect KiCad MCP server"
